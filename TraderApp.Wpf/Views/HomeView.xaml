<UserControl x:Class="TraderApp.Wpf.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TraderApp.Wpf.Views"
             xmlns:controls="clr-namespace:TraderApp.Wpf.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Home"
                   FontSize="24"
                   FontWeight="Bold"
                   Margin="0 10"/>
        
        <controls:MajorIndexListing Grid.Row="1"
                                    Margin="0 10"
                                    HorizontalAlignment="Center"
                                    DataContext="{Binding MajorIndexViewModel}"/>
    </Grid>
</UserControl>
