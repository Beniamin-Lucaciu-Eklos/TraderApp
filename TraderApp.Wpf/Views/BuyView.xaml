<UserControl x:Class="TraderApp.Wpf.Views.BuyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TraderApp.Wpf.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="booleanToVisbilityConverter"/>
    </UserControl.Resources>

    <Grid HorizontalAlignment="Center"
          Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition />
        </Grid.RowDefinitions>

        <WrapPanel Grid.Row="0"
                   HorizontalAlignment="Center">
            <TextBox Text="{Binding Symbol, UpdateSourceTrigger=PropertyChanged}"
                     Width="100"/>

            <Button Content="Search"
                    Command="{Binding SearchSymbolCommand}"
                    Margin="5 0 0 0"/>
        </WrapPanel>

        <TextBlock DataContext="{Binding ErrorMessageViewModel}"
                   Text="{Binding Message}" 
                   HorizontalAlignment="Center"
                   Margin="0 10 0 0"
                   Visibility="{Binding HasMessage, Converter={StaticResource booleanToVisbilityConverter}}"
                   Foreground="DarkRed"
                   Grid.Row="1"/>

        <TextBlock DataContext="{Binding StatusMessageViewModel}"
                   Text="{Binding Message}"
                   HorizontalAlignment="Center"
                   Margin="0 10 0 0"
                   Visibility="{Binding HasMessage, Converter={StaticResource booleanToVisbilityConverter}}"
                   Foreground="DarkGreen"
                   Grid.Row="2"/>

        <Grid Grid.Row="3"
              Margin="0 20 0 0"
              MaxWidth="600">
            <Grid.Style>
                <Style TargetType="Grid">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding SearchResultSymbol}"
                                     Value="">
                            <Setter Property="Visibility" Value="Collapsed"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Grid.Style>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>            
            
            <Border BorderBrush="Gray"
                    BorderThickness="1"
                    CornerRadius="3 0 0 3"
                    Padding="20"
                    Grid.Column="0">
                <StackPanel>
                    <TextBlock HorizontalAlignment="Center"
                               Text="{Binding SearchResultSymbol}"
                               FontSize="32"/>
                    <TextBlock Margin="0 10 0 0"
                               Text="{Binding StockPrice, StringFormat='{}{0:C}', FallbackValue='$0.00'}"/>
                </StackPanel>
            </Border>

            <Border BorderBrush="Gray"
                    BorderThickness="0,1,1,1"
                    CornerRadius="0 3 3 0"
                    Padding="20"
                    Grid.Column="1">
                <StackPanel HorizontalAlignment="Center">
                    <WrapPanel Margin="0 10">
                        <TextBlock Text="Shares to buy:" 
                                   Width="150"/>
                        <TextBox Width="50"
                                 Text="{Binding ShareToBuy, UpdateSourceTrigger=PropertyChanged}"/>
                    </WrapPanel>

                    <WrapPanel Margin="0 10">
                        <TextBlock Text="Price per share:"
                                   Width="150"/>
                        <TextBlock Text="{Binding StockPrice, StringFormat='{}{0:C}'}"
                                   Width="50"/>
                    </WrapPanel>

                    <TextBlock FontSize="32"
                               Margin="0 10"
                               Text="{Binding TotalPrice}"/>
                    
                    <Button Content="Buy"
                            Margin="0 10"
                            FontSize="24"
                            Padding="20 5"
                            Command="{Binding BuyStockCommand}" />
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
